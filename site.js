(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;document.querySelectorAll('#intro,.intro-overlay').forEach(x=>{x.style.pointerEvents='none';setTimeout(()=>x.remove(),700)});const slides=[...document.querySelectorAll('.hero-slide,.hero .slides>img,.hero-bg>img')];let i=0,paused=reduce;function show(){slides.forEach((s,n)=>{s.classList.toggle('on',n===i);s.classList.toggle('active',n===i);s.style.opacity=n===i?'1':'0'})}if(slides.length){show();setInterval(()=>{if(!paused&&!document.hidden){i=(i+1)%slides.length;show()}},5000)}const pause=document.querySelector('.slide-control');if(pause){pause.textContent=paused?'Play slideshow':'Pause slideshow';pause.onclick=()=>{paused=!paused;pause.textContent=paused?'Play slideshow':'Pause slideshow';pause.setAttribute('aria-pressed',String(paused))}}const b=document.querySelector('[data-menu-button]'),m=document.querySelector('[data-mobile-menu]');function close(){if(m){m.classList.remove('open','active');m.style.display='none'}if(b)b.setAttribute('aria-expanded','false')}if(b&&m){b.onclick=()=>{const open=b.getAttribute('aria-expanded')!=='true';b.setAttribute('aria-expanded',String(open));m.classList.toggle('open',open);m.style.display=open?'block':'none'};m.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));document.addEventListener('keydown',e=>{if(e.key==='Escape'){close();b.focus()}})}window.closeMobile=close;document.querySelectorAll('.reveal').forEach(x=>x.classList.add('in','visible'));const s=document.getElementById('baSlider');if(s){let drag=false,pos=50;s.tabIndex=0;s.setAttribute('role','slider');s.setAttribute('aria-label','Compare before and after haircut');s.setAttribute('aria-valuemin','2');s.setAttribute('aria-valuemax','98');function set(p){pos=Math.max(2,Math.min(98,p));s.style.setProperty('--pos',pos+'%');s.setAttribute('aria-valuenow',Math.round(pos))}set(50);s.onpointerdown=e=>{drag=true;s.setPointerCapture(e.pointerId);set((e.clientX-s.getBoundingClientRect().left)/s.clientWidth*100)};s.onpointermove=e=>{if(drag)set((e.clientX-s.getBoundingClientRect().left)/s.clientWidth*100)};s.onpointerup=s.onpointercancel=()=>drag=false;s.onkeydown=e=>{if(['ArrowLeft','ArrowRight'].includes(e.key)){e.preventDefault();set(pos+(e.key==='ArrowLeft'?-5:5))}}}document.querySelectorAll('#year').forEach(x=>x.textContent=new Date().getFullYear())})();